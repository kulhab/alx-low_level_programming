#include "main.h"
#include <stdio.h>

/**
 * add - check the code
 * @a: single value input
 * @b: single value input
 * Return: Always 0.
 */
int add(int a, int b)
{
	int result;
	result = a+b;
	return (result);
}
