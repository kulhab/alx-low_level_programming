#include "main.h"

/**
* main - tests function that prints if integer is positive or negative
* Return: 0
*/
int main(void)
{
	int i;
        i = 0;
	int r;
		r = positive_or_negative(i);
		printf("%d is zero",r);

	return (0);
}
